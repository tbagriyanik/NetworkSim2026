/** Exam generation API, kept separate from encryption/integrity consumers. */
export { generateExamFromProject, getExamProjects } from './examMode';
export type { ProjectData, ExamTask } from './examMode';
